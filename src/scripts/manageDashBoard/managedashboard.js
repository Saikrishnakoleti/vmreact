import React, { Component } from "react";

class ManageDashBoard extends Component {
  render() {
    return (
      <div className = "checked">
        <h4>This is ManageDashBoard</h4>
        <p>Hello India</p>
        <p>If you have an existing application</p> 
        <p>you'll need to install a few packages to make everything work well together.</p>
        <p> We are using the babel-jest package and the react babel preset to transform our code inside of the test environment. Also see using babel.</p>
      </div>
    );
  }
}
export default ManageDashBoard;
