import React, { Component } from "react";

class MyDashBoard extends Component {
  render() {
    return (
      <div>
        <h3>This is My Account</h3>
        <p>Wel come to your Account</p>
        <p>The root folder will be named reactApp and we will place it on Desktop.</p>
        <p> After the folder is created, we need to open it and create empty package.</p>
        <p>Json file inside by running npm init from the command prompt and follow the instructions.</p>
      </div>
    );
  }
}
export default MyDashBoard;
